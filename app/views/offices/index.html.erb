
<div class="container col-4 offset-4 mt-5">
  <h1>THE OFFICES</h1>
</div>
<div class="office-index-container">
  <div class="cards_display">
    <% @offices.each do |office| %>
      <%= render "office_card", office: office %>
    <% end %>
  </div>
</div>
