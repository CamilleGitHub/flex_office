<div class="card-trip">
  <%= cl_image_tag office.photo.key, height: 300, width: 400, crop: :fill %>
  <div class="card-trip-infos">
    <div>
      <h1><%= office.name %></h1>
      <h3><%= office.address %></h3>
      <p><%= office.city %></p>
    </div>
    <div>
      <h2 class="card-trip-pricing"><%= office.capacity%>   <i class="fas fa-users"></i></h2>
      <h2 class="card-trip-pricing"><%= office.price %> € / day </h2>
    </div>
  </div>
</div>